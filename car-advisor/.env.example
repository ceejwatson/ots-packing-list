# Required for listing extraction, the written summary, and follow-up questions.
# Without it the app still runs: regex extraction and every deterministic model
# work offline, and you get a computed summary instead of a written one.
ANTHROPIC_API_KEY=sk-ant-...
